MPLBACKEND=Agg

# Name of the conda environment to use for running tests.
# Must match the environment selected in VS Code (python.defaultInterpreterPath).
# Used by conda_python.cmd (Windows) and conda_python.sh (Linux/Mac).
CONDA_ENV_NAME=glaes_env
