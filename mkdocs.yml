site_name: ETHOS.GLAES
site_url: https://ethosglaes.readthedocs.io/
repo_url: https://github.com/FZJ-IEK3-VSA/glaes
repo_name: FZJ-IEK3-VSA/glaes
edit_uri: edit/master/docs/

theme:
  name: material
  custom_dir: docs/overrides
  # NOTE: no `favicon:` key — it previously pointed at assets/favicon.svg, which
  # has never existed in the repo (docs/assets/ is empty and untracked), so every
  # page requested a 404. Material's own default favicon is used until GLAES has
  # a brand mark of its own; add the key back alongside a `logo:` when it does.
  font:
    text: Roboto
    code: Roboto Mono
  features:
    - navigation.instant
    - navigation.instant.prefetch
    - navigation.instant.progress
    - navigation.tabs
    - navigation.sections
    - navigation.indexes
    - navigation.top
    - navigation.path
    - navigation.tracking
    - content.code.copy
    - content.code.annotate
    - content.tabs.link
    - content.action.edit
    - content.action.view
    - content.tooltips
    - search.suggest
    - search.highlight
    - search.share
    - toc.follow
  palette:
    - scheme: default
      primary: custom
      accent: custom
      toggle:
        icon: material/brightness-7
        name: Switch to dark mode
    - scheme: slate
      primary: custom
      accent: custom
      toggle:
        icon: material/brightness-4
        name: Switch to light mode

nav:
  - Home:
    - index.md
    - Installation: installation.md
    - Getting Started: getting-started.md
  - Examples:
    - Basic Workflow: notebooks/00_basic_workflow.ipynb
    - Placement Algorithm: notebooks/01_Placement_algorithm.ipynb
    - Extended Methods: notebooks/02_extended_methods.ipynb
  - API Reference: api/
  - About:
    - Further Reading: further-reading.md
    - Legal Notice: legal-notice.md

hooks:
  - docs/copy_examples.py

# docs/overrides/ holds the Material theme overrides (see theme.custom_dir); it
# is template source, not content, so it must not be copied into the site.
exclude_docs: |
  gen_ref_pages.py
  copy_examples.py
  execute_notebooks.py
  overrides/

plugins:
  - search
  - mkdocstrings:
      handlers:
        python:
          paths: [.]
          options:
            show_source: true
            show_root_heading: true
            members_order: source
            docstring_style: auto
            show_symbol_type_heading: true
            show_symbol_type_toc: true
            separate_signature: true
            show_signature_annotations: true
  - mkdocs-jupyter:
      # Notebooks are NOT executed here. They are pre-executed in parallel by
      # docs/execute_notebooks.py as a docs pre-build step (see
      # .readthedocs.yaml) and rendered from those outputs, which keeps the
      # build fast and surfaces notebook failures on their own.
      execute: false
      include_source: true
      allow_errors: false
      include_requirejs: true
      toc_depth: 3
  - gen-files:
      scripts:
        - docs/gen_ref_pages.py
  - literate-nav:
      nav_file: SUMMARY.md

markdown_extensions:
  - pymdownx.arithmatex:
      generic: true
  - pymdownx.highlight:
      anchor_linenums: true
      line_spans: __span
  - pymdownx.superfences:
      custom_fences:
        - name: mermaid
          class: mermaid
          format: !!python/name:pymdownx.superfences.fence_code_format
  - pymdownx.tabbed:
      alternate_style: true
  - pymdownx.emoji:
      emoji_index: !!python/name:material.extensions.emoji.twemoji
      emoji_generator: !!python/name:material.extensions.emoji.to_svg
  - pymdownx.inlinehilite
  - pymdownx.snippets
  - pymdownx.mark
  - admonition
  - pymdownx.details
  - attr_list
  - md_in_html
  - tables
  - def_list
  - footnotes
  - abbr
  - toc:
      permalink: true
      toc_depth: 2

extra:
  generator: false

extra_css:
  - stylesheets/extra.css
