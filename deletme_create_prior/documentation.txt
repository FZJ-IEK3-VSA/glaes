
#### Overall Plan  ####

- create a more generic prior function (start with function that identifies rather a tif or shp file is used)
    -remove all the hard coded stuff from severin in: /glaes/create_prior.py
    -write an example for using prior
- remove the prior from the example
- create a new only prior example

NÄCHSTE SCHRITTE
-----------------
- Create Priors müssten jetzt soweit laufen und getestet werden (innerhab des 0 create priors file)
- dann müssen pfade zu den einzelnen tif und shape files als argumente eingebaut und übergeben werden
- wenn das funktioniert, die funktionen generischer aufbauen und eine Funktion davor schreiben, die entscheidet ob tif oder shape
- Einzelfälle abdecken. Severin hat tausend einzelfälle, wie müssen die abgedeckt werden




GLAES PRIORS SCTRUCTURE
------------------------
# testen der priors in: /fast/home/l-madeisky/models_IEK_3/IEK3_Models/ethos-installation/ethos_suite_repositories/glaes/glaes/test/test_priors.py
# Beispielprior: priorSample = gl._test_data_["roads_prior_clip.tif"]

# 0 create priors
hier finden sich die funktionen, um die priors zu erzeugen
    Ziel: #generische function
        #1 
            funktion zum erkennen ob raster oder vector data
            try to load raster, if not ...
            if vector data ... if raster data

        #2 
            generische evaluate funktion 
            '''

# 1 run create priors
dieses scrpt gibt es nicht von severin, wird genutzt, um die 
funktionen aus #0 laufen zu lassen

# priors class
hier werden priors geladen und manipuliert
auf diesen Teil beziehen sich auch die prior test_priors


Allemeine create priors Aufbau
---------------------------------------------
- 1. region shape oder raster wird als region mask geladen
- 2. bei tifs : region mask und tif file werden übergeben // tif file wird noch prozessiert mit indicate values
     bei shape: region mask wird übergeben, geoms von shape werden eingelesen und übergeben //
- 3. edgesByThreshold: ein tif file wird erzeugt, dass für jede kateorie aus dem evaluation dict eine nummer (value vergibt)
     edgesByProximity: nutzt in einem ersten schritt die grow function um die mask zu buffern